# 小白服务器管理器功能计划书

版本：v1.2 Oracle TNS 完整验收版  
更新时间：2026-09-04  
当前状态：首期核心功能已完成，Redis 与 Oracle 数据库端口修改已完成真实往返和协议级验收

## 一、项目定位

小白服务器管理器是一款运行在本地 Windows 电脑上的轻量服务器管理工具，主要用于集中管理多台 RDP / SSH 服务器。

核心目标：

- 集中保存和管理多台服务器资料。
- 实时查看服务器连通状态和延迟。
- 快速连接 RDP、SSH。
- 通过服务器已有的 SSH 或 WinRM 直连执行远程操作。
- 不部署 Agent，不使用 Relay 或中转服务器。
- 用尽量少的界面空间提供清晰、直接的操作。

明确不采用：

- Agent 服务。
- Relay 或中转服务器。
- PsExec 随程序发布。
- 为执行重启命令而在每台服务器上部署额外组件。

## 二、首期功能完成情况

| 功能 | 状态 | 说明 |
|---|---|---|
| 服务器列表管理 | 已完成 | 名称、IP、端口、账号、分组、厂商、备注、到期时间 |
| 实时延迟检测 | 已完成 | Ping 与服务 TCP 端口检测，30 秒自动刷新 |
| RDP 连接 | 已完成 | 使用 Windows 远程桌面 |
| SSH 连接 | 已完成 | 调用 PuTTY 或系统 SSH |
| 启动密码锁 | 已完成 | 首次设置，后续启动验证 |
| 存储模式选择 | 已完成 | 明文 XML 与加密保险库二选一，均不默认选中 |
| 明文兼容模式 | 已完成 | 全部服务器资料写入 servers.xml |
| 加密保险库模式 | 已完成 | 全部服务器资料写入 servers.vault |
| 正常修改管理密码 | 已完成 | 不删除服务器资料 |
| 强制彻底重置 | 已完成 | 删除全部资料和凭据，恢复首次使用状态 |
| 远程重启 | 已完成 | SSH 优先，WinRM 备用，已完成真实测试 |
| RDP 端口修改 | 已完成 | 注册表、服务、防火墙、新端口验证和回滚 |
| SSH 端口修改 | 已完成 | 旧端口与新端口双监听，独立连接验证后清理旧端口 |
| HTTP/HTTPS 引导修改 | 已完成 | IIS 站点绑定支持自动修改，其他 Web 服务不盲改 |
| MySQL / MariaDB | 已完成 | Windows 服务和 my.ini 端口修改 |
| Redis | 已完成 | Windows 服务配置端口修改 |
| MongoDB | 已完成 | Windows 服务配置端口修改 |
| Oracle XE | 已完成 | Listener、LOCAL_LISTENER、动态注册、TNS 服务验证和完整回滚 |
| 新端口快速探测 | 已完成 | 点击输入框后读取服务器监听端口 |
| 随机空闲端口 | 已完成 | 优先选择 10000 以上的空闲端口 |
| 本机 IP 防火墙检查 | 已完成 | 从远程会话取得服务器实际看到的来源 IP |
| 失败自动回滚 | 已完成 | 恢复配置、防火墙规则和服务状态 |

## 三、远程管理架构

### 3.1 管理通道

管理器只使用服务器已有的远程管理入口：

1. 自动模式：优先尝试 SSH。
2. SSH 连接失败时，Windows 服务器回退 WinRM。
3. SSH 模式：只使用 SSH，失败直接提示。
4. WinRM 模式：只使用 WinRM，失败直接提示。
5. RDP 仅用于桌面登录和状态检测，不作为远程命令执行协议。

管理器不会自动安装 Agent，也不会要求服务器连接中转服务。

### 3.2 Windows 远程执行

- SSH 使用 SSH.NET 执行命令。
- Windows SSH 会话中执行 PowerShell。
- WinRM 使用 Windows PowerShell Remoting。
- 密码不会写入命令行参数、脚本文件或日志。
- 新版存储模式下，服务器密码保存在当前选定的资料存储中。

### 3.3 实际限制

如果服务器既没有 SSH，也没有 WinRM，管理器无法凭空获得远程命令执行能力。此时只能通过云厂商控制台、VNC、串口或带外管理处理。

## 四、启动锁和存储模式

### 4.1 首次启动

首次启动流程：

~~~text
设置管理主密码
    ↓
必须选择存储模式
    ↓
创建对应的服务器资料存储
    ↓
进入管理器
~~~

存储模式窗口不默认选中任何选项。用户取消选择时不能直接进入管理器。

### 4.2 明文兼容模式

文件：servers.xml

服务器名称、IP、端口、用户名、密码、分组、备注和其他资料均以明文 XML 保存。

适用场景：

- 个人可信电脑。
- 需要直接查看或编辑 XML。
- 兼容旧工具或旧数据。

风险：任何能够读取该文件的人都可以直接看到服务器凭据，不建议放入 U 盘、网盘、邮件或共享目录。

### 4.3 加密保险库模式

文件：servers.vault

全部服务器资料使用 AES-256-GCM 加密保存，包括服务器密码、IP、端口和备注。

管理主密码同时承担两个作用：

1. 解锁管理器。
2. 派生保险库加密密钥并解密服务器资料。

技术规则：

- 使用随机盐值。
- 使用随机 nonce。
- 使用 PBKDF2 派生 256 位密钥。
- 使用 AES-256-GCM 提供加密和完整性校验。
- 错误密码或文件被篡改时拒绝打开。
- 不依赖本机 Windows 凭据管理器，适合便携使用。

### 4.4 模式互斥

程序同一时间只允许一种活动存储文件：

- 明文模式使用 servers.xml。
- 加密模式使用 servers.vault。

如果同时检测到两个文件，程序不会自行猜测哪一个是正确数据，而是要求执行彻底重置。

### 4.5 正常改密

正常修改管理密码不会删除服务器资料：

- 明文模式：使用新密码哈希重新写入 servers.xml。
- 加密模式：使用新密码重新派生密钥并重新加密 servers.vault。

### 4.6 强制彻底重置

强制重置不等于恢复默认密码后继续使用，而是完整清空软件：

- 删除 servers.xml。
- 删除 servers.vault。
- 删除服务器凭据。
- 删除 RDP 登录缓存。
- 删除旧管理密码和遗留密码文件。
- 不保留服务器列表、端口、备注或任何其他服务器信息。

重置完成后，下一次启动等同于第一次使用，必须重新设置密码并选择存储模式。

忘记加密保险库主密码时，无法恢复原保险库内容；强制重置只能清空并重新开始。

## 五、实时状态和界面

首期界面设计原则：

- 左侧灰色区域按列显示单台服务器实时状态。
- 顶部横向显示服务器总数、在线数、异常数和到期数。
- 主列表用颜色区分状态和延迟。
- 底部使用明显的操作按钮。
- 保留鼠标右键操作菜单。
- 重启和端口修改使用独立二级操作窗口。
- IP 默认完全隐藏，显示后 5 秒自动隐藏。

默认 IP 显示：

~~~text
***.***.***.***:****
~~~

点击“显示 IP”并通过管理密码验证后，才显示完整地址和端口。

## 六、远程重启

### 6.1 执行逻辑

重启流程保持简单：

~~~text
二次确认
    ↓
连接并验证权限
    ↓
执行 shutdown.exe /r
    ↓
确认 shutdown.exe 真实退出码
    ↓
等待管理通道短暂中断
    ↓
重新连接并检查系统启动时间变化
~~~

命令提交成功不等于重启成功。只有管理通道恢复，并且系统启动时间晚于重启前记录，才显示成功。

### 6.2 进度窗口

重启窗口只显示四个阶段：

1. 连接并验证权限。
2. 发送重启命令。
3. 等待服务器重新启动。
4. 确认服务器恢复。

正常超时时间约 90 秒，适配大多数服务器 30–50 秒的重启时间。

### 6.3 关机功能

首期不实现关机按钮。关机后无法通过本地管理器开机，通常还需要进入云厂商后台，实际使用价值较低。

## 七、常规端口管理

### 7.1 新端口输入

新端口输入框初始为只读状态。用户点击输入框时：

1. 通过当前 SSH/WinRM 通道读取服务器监听中的 TCP 端口。
2. 排除当前服务端口。
3. 检测完成后解锁输入框。
4. 用户可以手动输入端口。
5. 执行修改前再次检查端口占用，防止扫描结果过期。

输入框旁的“随机”按钮会从空闲端口中选择端口，优先使用 10000 以上端口。

### 7.2 本机 IP 防火墙检查

开始修改前，程序从当前远程会话中读取服务器实际看到的客户端来源 IP：

- SSH 使用 SSH_CONNECTION。
- WinRM 使用会话信息。

程序不会直接读取本地网卡地址，也不依赖第三方公网 IP 查询。

如果服务器防火墙未允许该来源 IP 访问新端口，程序会先创建只允许该 IP 的规则，再开始端口修改。原有防火墙规则不会删除。

### 7.3 RDP 端口

RDP 修改流程：

1. 检查新端口占用。
2. 检查本机 IP 防火墙通行。
3. 备份 RDP 注册表端口。
4. 创建程序自己的防火墙规则。
5. 修改 PortNumber。
6. 重启 TermService。
7. 检测新端口。
8. 成功后更新本地记录。
9. 失败时恢复注册表、防火墙和服务。

整个过程通过 SSH 或 WinRM 执行，不依赖 RDP 连接本身。

### 7.4 SSH 端口

例如把 SSH 22 修改为 33：

~~~text
通过 22 连接
    ↓
检查 33 是否占用
    ↓
开放 33 防火墙规则
    ↓
临时配置 Port 22 + Port 33
    ↓
检查 sshd 配置并重启服务
    ↓
新建独立 SSH 连接验证 33
    ↓
通过 33 删除 22
    ↓
再次通过 33 验证
    ↓
更新本地记录
~~~

不额外使用 222 作为固定维护端口，因为保留旧端口直到新端口验证成功已经足够。

如果新端口连接失败，程序保留旧端口并尝试回滚。若 SSH 服务完全无法启动，仍需使用 WinRM、VNC 或云厂商控制台恢复。

### 7.5 HTTP / HTTPS

HTTP/HTTPS 不进行全局数字替换。

修改前必须提示用户确认：

- 实际运行的 Web 服务。
- 实际配置文件或 IIS 站点绑定中的当前端口。
- 是否存在多个站点、反向代理或容器映射。

首期支持：

- IIS HTTP 站点绑定。
- IIS HTTPS 站点绑定。

对于 Nginx、Apache、Caddy、Docker 等无法明确识别配置目标的情况，只显示提示，不自动修改。

## 八、数据库端口管理

首期数据库范围：

- MySQL。
- MariaDB。
- Redis。
- MongoDB。
- Oracle XE（用于当前端口修改测试）。

统一流程：

~~~text
识别服务
    ↓
读取实际配置
    ↓
检查新端口和本机 IP 通行
    ↓
备份配置
    ↓
修改配置
    ↓
重启服务
    ↓
验证新端口
    ↓
成功更新记录，失败自动回滚
~~~

当前首期主要完成 TCP 监听验证。是否进一步调用 mysqladmin、redis-cli 或 mongosh 做业务级验证，作为后续增强项。

Oracle XE 的完整流程：

1. 读取 Listener 服务、Oracle Home、listener.ora 和数据库服务名。
2. 从 `DB_DOMAIN` 与活动服务生成完整服务名，例如 `xepdb1.localdomain`。
3. 只替换 listener.ora 中 TCP 地址的端口，不修改 IPC 等其他端口字段。
4. 重启 Oracle Listener。
5. 使用本机 `sqlplus / as sysdba` 更新 `LOCAL_LISTENER`。
6. 执行 `ALTER SYSTEM REGISTER`，让 XE/XEPDB1 重新注册到新端口。
7. 从管理电脑发送真实 Oracle TNS CONNECT 请求，确认 PDB/数据库服务已被 Listener 识别。
8. 任一步失败时恢复 listener.ora、原 `LOCAL_LISTENER`、服务注册和防火墙规则。

PowerShell 写入统一使用无 BOM UTF-8。长脚本经 GZip 压缩后通过 SSH 在远端内存解压执行，不生成额外脚本文件。

## 九、回滚和安全边界

### 9.1 修改前

- 保存旧端口。
- 保存服务名。
- 保存配置路径。
- 保存配置备份。
- 记录程序创建的防火墙规则。
- 不修改本地服务器记录。

### 9.2 修改中

- 先检查新端口占用。
- 先确保当前管理电脑可以访问新端口。
- 修改连接类端口时保留旧通道。
- 每个阶段设置超时。
- 界面显示实际执行阶段。

### 9.3 修改成功

只有满足以下条件才更新本地资料：

- 服务已重启或重新加载。
- 新端口可以访问。
- 服务状态正常。
- SSH 迁移已通过新端口建立独立连接。

### 9.4 修改失败

程序尝试：

- 恢复原配置。
- 恢复原端口。
- 删除程序创建的新规则。
- 重启或重新加载原服务。
- 验证旧端口恢复。

如果服务器已经完全失联，程序只能明确提示：

~~~text
远程连接已中断，自动回滚未确认。
请通过云厂商控制台、VNC、串口或带外管理恢复。
~~~

## 十、当前暂缓功能

以下功能不属于首期验收范围：

1. Linux 端口管理和 Linux 防火墙自动适配。
2. PostgreSQL。
3. SQL Server。
4. Oracle 非 XE 复杂实例和多监听器场景。
5. ClickHouse。
6. Elasticsearch。
7. VNC 自动修改。
8. 自定义 TCP/UDP 服务自动修改。
9. Nginx、Apache、Caddy 的自动配置。
10. Docker 容器内部端口自动修改。
11. 批量端口修改。
12. 完整操作历史和审计报表。
13. 失败任务批量重试。

这些功能后续按实际使用频率逐项增加，不改变当前本地直连架构。

## 十一、测试和验收记录

### 11.1 Windows 测试服务器

已验证：

- Windows Server 2019。
- SSH 连接和 PowerShell 执行。
- 管理员身份识别。
- RDP 端口识别。
- SSH 端口识别。
- MySQL 端口识别。
- 远程重启、断线和恢复。
- 系统启动时间变化判断。
- shutdown.exe 真实退出码检查。
- Redis 6379 → 临时端口 → 6379 往返修改和恢复。
- Oracle Listener 1521 → 临时端口 → 1521 往返修改和恢复。
- Oracle 26130 → 31711 → 26130 往返修改，`xepdb1.localdomain` TNS 服务验证通过。
- Oracle `LOCAL_LISTENER` 与 `ALTER SYSTEM REGISTER` 同步更新通过。
- TCP 占用判断只检查 `Listen`，不再把 `TIME_WAIT` 误判为占用。
- 修改失败时清理程序创建的临时防火墙规则。

### 11.2 存储测试

已验证：

- AES-256-GCM 保险库保存和读取。
- 正确主密码可以解密。
- 错误主密码被拒绝。
- 加密文件中搜索不到明文服务器密码。
- 明文 XML 可以保存和读取服务器密码。
- 密钥和盐值参数可以在保存后正确匹配。

### 11.3 编译发布

- .NET 8 WinForms。
- win-x64 自包含发布。
- 单文件发布包约 169 MB。
- 发布包不包含服务器资料文件。
- 服务器资料在用户选择模式后才生成。

## 十二、后续路线

推荐后续顺序：

1. 完成首期端口功能实际验收。
2. 根据测试反馈修正回滚和提示细节。
3. 增加 Linux SSH 端口和防火墙适配。
4. 增加 PostgreSQL。
5. 增加批量操作和操作历史。
6. 根据实际需求扩展 Web 服务和其他数据库。

## 十三、最终结论

首期目标已经从“复杂的全能服务器平台”收敛为：

~~~text
本地运行
无需 Agent
无需 Relay
SSH / WinRM 直连
RDP / SSH / 常用数据库端口管理
简单可靠的重启验证
明文兼容模式
AES-256-GCM 便携保险库模式
~~~

该范围已经覆盖当前实际使用需求，剩余内容主要是 Linux、更多服务类型、批量能力和审计功能等后续扩展。
