# 小白服务器管理器：数据库管理模块界面与功能设计方案

版本：v1.4 数据库综合功能实现稿  
更新时间：2026-09-04  
当前阶段：MySQL / MariaDB、Redis ACL、MongoDB 用户角色及备份入口已接入，进入综合验收

## 一、设计目标

在现有服务器管理器底部操作区增加“数据库管理”按钮，进入独立的数据库管理模块。

本模块主要用于：

- 查看服务器上已安装的数据库服务。
- 保存和验证数据库管理凭据。
- 管理数据库用户和权限。
- 执行数据库备份、迁移和恢复。
- 在不开放数据库外网端口的情况下，通过现有 SSH 通道完成管理。

核心原则：

- 不部署 Agent。
- 不使用 Relay 或中转服务器。
- 不要求 MySQL、MariaDB、MongoDB 或 Redis 开放公网端口。
- 数据库凭据与服务器登录凭据分开管理。
- 数据库密码只允许加密保存到 `servers.vault`。
- Oracle 首期只预留入口，不实现实际管理逻辑。

## 二、开发顺序建议

建议采用“界面先行、逻辑分阶段填充”的方式，而不是边做面板边写完整数据库逻辑。

推荐流程：

~~~text
确定模块入口和按钮
    ↓
确定数据库总览面板
    ↓
确定 MySQL / MariaDB 独立面板
    ↓
确定 MongoDB、Redis 面板结构
    ↓
确定凭据录入、用户管理、迁移窗口
    ↓
冻结界面和交互
    ↓
逐个填充连接、用户、权限、迁移逻辑
~~~

这样做的好处：

1. 先确定信息层级，避免功能增加后反复调整窗口布局。
2. MySQL、MongoDB、Redis 的权限模型不同，可以共用视觉规范，但不强行共用错误的操作界面。
3. 先使用模拟数据确认按钮、列表、弹窗和状态提示，后续接入真实数据库时改动更小。
4. 每个功能可以独立验收，出现问题时更容易定位是界面问题还是数据库适配问题。
5. 可以先完成可操作的界面原型，不会因为某一种数据库的复杂逻辑拖慢整体设计。

## 三、主界面入口

在现有主界面底部操作按钮区域增加：

~~~text
[添加服务器] [连接] [编辑] [重启] [端口管理] [刷新状态] [显示 IP] [更多操作] [数据库管理]
~~~

“数据库管理”按钮启用条件：

- 已选择服务器。
- 服务器已配置可用的 SSH 或 WinRM 管理通道。
- 当前服务器类型为 Windows，或后续已实现对应 Linux 适配。

如果没有检测到数据库，不直接报错，而是进入数据库管理窗口并显示：

~~~text
未检测到已支持的数据库服务
    [重新检测]    [手动添加数据库]
~~~

## 四、数据库管理主窗口

数据库管理主窗口采用独立二级窗口，不占用主服务器列表空间。

建议窗口标题：

~~~text
数据库管理 · 服务器名称
~~~

### 4.1 顶部概览区

顶部横向显示当前服务器和数据库状态：

~~~text
服务器：8-8-50        管理通道：SSH        状态：在线
数据库服务：3          正常：3             异常：0
~~~

### 4.2 数据库服务列表

列表展示已检测到的数据库：

| 状态 | 类型 | 服务名称 | 版本 | 地址 | 端口 | 凭据状态 | 操作 |
|---|---|---|---|---|---:|---|---|
| 正常 | MySQL | MySQL80 | 8.0 | 127.0.0.1 | 3306 | 已验证 | 管理 |
| 正常 | Redis | xiaobairedis | 版本 | 127.0.0.1 | 51758 | 已验证 | 管理 |
| 正常 | Oracle | Listener | 21c XE | 127.0.0.1 | 26130 | 暂不支持 | 开发中 |

IP 默认隐藏，端口可以正常显示在数据库管理窗口中；如涉及敏感信息，仍应沿用主界面的显示策略。

### 4.3 底部按钮

~~~text
[刷新检测] [添加数据库] [连接测试] [关闭]
~~~

选择数据库后，点击“管理”进入对应数据库的专用二级面板。

## 五、数据库专用面板

每种数据库单独使用自己的界面类和操作面板，不共用一个强行统一的权限界面。

建议结构：

~~~text
MySQLManagementForm
MariaDbManagementForm
MongoDbManagementForm
RedisManagementForm
OracleManagementForm（占位）
~~~

视觉风格可以统一，但字段、权限、按钮和提示内容根据数据库类型分别设计。

## 六、MySQL / MariaDB 面板

### 6.1 面板结构

~~~text
数据库概览 | 用户与权限 | 备份与迁移 | 连接设置
~~~

数据库概览显示：

- 数据库类型和版本。
- 当前连接地址和端口。
- 服务运行状态。
- 当前登录账号。
- 当前连接方式：SSH 隧道或远程命令。
- 数据库数量和字符集信息。

### 6.2 首次凭据录入

首次管理时弹出“数据库凭据验证”窗口：

~~~text
数据库类型：MySQL
连接地址：127.0.0.1
端口：3306
管理用户名：[ root              ]
管理密码：  [                  ]
认证数据库：[ 默认             ]

[测试连接并保存] [取消]
~~~

验证成功后：

- 将凭据保存到 `servers.vault`。
- 不写入命令行参数。
- 不写入脚本文件。
- 不写入日志。
- 后续管理操作自动读取加密凭据。

数据库凭据应与服务器凭据使用独立记录，例如保存数据库类型、服务标识、用户名、密码、认证库和最后验证时间。

### 6.3 用户与权限

用户列表展示：

| 用户名 | 来源主机 | 权限范围 | 状态 | 最后验证 | 操作 |
|---|---|---|---|---|---|
| root | localhost | 管理员 | 正常 | 今天 | 编辑 |
| app_user | % | 指定数据库读写 | 正常 | 今天 | 编辑 |

新增用户窗口：

~~~text
用户名：[ app_user                 ]
密码：  [                         ] [生成密码]
确认密码：[                       ]

授权范围：
□ 指定数据库
□ 读取数据
□ 新增和修改数据
□ 删除数据
□ 创建和修改表结构
□ 执行存储过程
□ 创建数据库
□ 管理其他用户
□ 全部权限

来源主机：○ localhost   ○ 指定 IP   ○ %

[预览 SQL] [创建并验证] [取消]
~~~

设计要求：

- 默认不勾选高危权限。
- 勾选“全部权限”或“管理其他用户”时二次确认。
- 生成用户后，先使用新账号测试登录。
- 新账号验证成功后，才保存用户名、密码和权限信息到保险库。
- 创建失败时不得保存半成品凭据。

### 6.4 备份与迁移

首期优先支持“远程数据库迁移到本地”：

~~~text
选择远程数据库
    ↓
选择数据库或数据表
    ↓
选择本地目标地址
    ↓
检查本地客户端和磁盘空间
    ↓
远程导出并压缩
    ↓
通过 SSH/SFTP 传输
    ↓
本地导入
    ↓
校验结果
~~~

迁移窗口建议显示：

- 源数据库信息。
- 目标数据库信息。
- 选择的数据库。
- 预计备份大小。
- 当前阶段。
- 传输进度。
- 导入进度。
- 成功、失败或跳过数量。
- 临时文件清理状态。

首期不自动删除用户确认保留的备份文件；默认清理程序产生的临时文件。

## 七、MongoDB 面板

MongoDB 使用独立用户和角色界面，不套用 MySQL 的权限名称。

建议权限选项：

- `read`：读取指定数据库。
- `readWrite`：读写指定数据库。
- `dbAdmin`：数据库管理。
- `userAdmin`：用户管理。
- `clusterAdmin`：集群管理。
- 自定义角色。

凭据窗口需要增加：

- 认证数据库，默认可填 `admin`。
- 用户所属数据库。
- 认证机制，首期可使用驱动默认值。

迁移使用 MongoDB 官方工具：

- `mongodump` 导出。
- `mongorestore` 导入。

## 八、Redis 面板

Redis 不采用传统数据库的“用户授权”模型，使用 ACL 用户和命令权限。

建议面板功能：

- 查看 Redis 版本和运行状态。
- 查看 ACL 用户。
- 新建 ACL 用户。
- 设置密码。
- 设置允许或禁止的命令。
- 设置 Key 访问范围。
- 设置只读或读写权限。
- 查看当前连接数量和内存使用。

Redis 用户权限可以提供预设模板：

- 只读用户。
- 普通读写用户。
- 指定 Key 前缀用户。
- 运维管理员。

迁移使用 Redis RDB/AOF 机制，不能直接套用 MySQL 导出导入流程。

## 九、Oracle 预留接口

Oracle 面板暂时只保留入口和基本说明：

~~~text
Oracle 数据库管理功能正在开发中

当前已支持 Oracle Listener 端口管理，
用户、权限、备份和迁移功能将在后续版本提供。

[关闭]
~~~

点击 Oracle 的“管理”按钮时不建立数据库管理连接，不执行修改操作。

## 十、SSH 隧道与连接方式

数据库管理优先使用现有 SSH 通道建立临时隧道：

~~~text
本机 127.0.0.1:随机端口
        ↓ SSH 加密隧道
服务器 127.0.0.1:数据库端口
~~~

操作结束后自动关闭隧道。

这样可以管理只监听本机地址的数据库，不需要开放数据库公网端口。

如果服务器禁止 SSH 端口转发：

- 优先尝试通过 SSH 执行数据库官方命令行工具。
- 如果命令行工具不存在，提示用户安装客户端或手动处理。
- 不自动修改数据库监听地址。

## 十一、数据库凭据存储设计

数据库凭据不放入 `ServicePortRecord`，应作为独立的保险库数据保存。

建议记录：

- 服务器凭据 ID。
- 数据库类型。
- 数据库服务名称。
- 地址和端口。
- 管理用户名。
- 管理密码。
- 认证数据库或实例名。
- 用户权限信息。
- 最后验证时间。
- 凭据状态。

新增用户记录建议保存：

- 数据库凭据 ID。
- 用户名。
- 密码。
- 来源主机或认证范围。
- 数据库名。
- 权限摘要。
- 创建时间。
- 最后验证时间。

所有密码继续由现有保险库统一加密。密码不出现在界面列表、日志、异常弹窗或临时脚本中。

如果用户选择明文 `servers.xml` 模式，数据库凭据保存前必须明确提示风险；更安全的做法是数据库凭据仅允许保存在 `servers.vault` 模式中。

## 十二、界面阶段的验收标准

在真实逻辑开发前，界面阶段需要先确认：

1. 主界面的“数据库管理”按钮位置和大小。
2. 数据库管理主窗口的尺寸和布局。
3. 数据库服务列表字段是否足够。
4. MySQL / MariaDB 用户列表和新增用户窗口。
5. MongoDB 用户与角色窗口。
6. Redis ACL 用户窗口。
7. 迁移窗口的步骤和进度显示。
8. 凭据首次录入和已保存状态显示。
9. Oracle“开发中”提示窗口。
10. 所有确认、取消、失败和成功状态的文案。

界面阶段可以使用模拟数据，不连接真实数据库，不修改服务器。

## 十三、已接入的第一批真实逻辑

目前已完成 MySQL / MariaDB 的基础管理能力：

- 数据库凭据验证并保存到 `servers.vault`。
- 通过 SSH 本地端口转发访问仅监听服务器本机的数据库。
- MySQL 8 `caching_sha2_password` 认证兼容。
- 读取数据库列表和用户列表。
- 新建用户、权限勾选、创建后登录验证。
- 编辑数据库级授权范围。
- 重置用户密码并用新密码验证。
- 删除用户和保险库记录同步。
- 远程数据库导出到本机 `.sql` 或 `.sql.gz` 备份文件。
- 迁移前检测本机数据库客户端和本机数据库端口。
- 本机没有数据库时分流到“仅导出备份”。
- 本机有目标数据库时进入本机账号验证和导入流程。
- 远程、本机临时凭据文件和不完整备份会在任务结束后清理。
- 支持从本机 `.sql` / `.sql.gz` 备份恢复到本机数据库。
- 本机数据库安装入口支持用户选择官方安装包后手动确认启动。
- 导入前校验文件非空、GZip 格式和目标数据库内容标识。
- 迁移导出默认不覆盖本机同名表；用户勾选覆盖后才加入 `DROP TABLE IF EXISTS`。

当前实现的实际界面流程：

~~~text
备份数据库
    ↓
读取远程非系统数据库列表
    ↓
勾选数据库和备份对象
    ↓
选择 .sql / .sql.gz 保存路径
    ↓
SSH 远程导出并传输
    ↓
校验并保存
~~~

~~~text
开始迁移 / 恢复备份
    ↓
检测本机数据库客户端和监听端口
    ├── 未检测到：仅导出备份 / 启动官方安装包 / 其他目标（预留）
    └── 检测到：验证本机数据库账号
                    ↓
                选择数据库
                    ↓
                选择是否覆盖本机同名表
                    ↓
                导入并显示真实传输进度
~~~

当前备份和迁移第一版范围：

- 远程导出优先查找 `mariadb-dump.exe` 或 `mysqldump.exe`。
- 备份默认排除系统库，避免把 MySQL 系统权限表直接覆盖到本地。
- 迁移默认使用 `--databases` 导出内容并导入本机目标。
- 本机没有 MySQL / MariaDB 时可保留备份文件，安装数据库后再导入。
- 本机数据库安装向导和其他目标服务器迁移入口暂时保留为后续功能。

本机数据库安装不静默下载、不静默安装。用户需要从官方渠道取得安装包，在安装向导中选择文件并确认启动；安装结束后重新检测本机服务。

## Redis ACL 第一版实际实现

- 通过 SSH 隧道访问 Redis 本地端口，不要求开放 Redis 公网端口。
- 使用内置 RESP 协议客户端，不要求本机安装 `redis-cli`。
- 验证 Redis 密码和 ACL 用户。
- 读取 `ACL LIST` 用户列表。
- 创建 ACL 用户并设置密码、Key 范围和命令类别。
- 创建后使用新账号发送 `PING` 验证登录。
- 编辑命令类别和 Key 范围。
- 重置 ACL 用户密码并验证。
- 删除普通 ACL 用户。
- 保护 `default` 用户和当前管理账号。
- ACL 修改成功后执行 `ACL SAVE`，必要时回退到 `CONFIG REWRITE`。
- Redis 低于 6.0 或不支持 ACL 时明确提示，不执行兼容性猜测操作。

Redis 的权限预设使用 `@read`、`@write`、`@connection`、`@transaction`、`@pubsub`、`@scripting` 和 `@admin` 命令类别；“全部命令”和管理类权限需要二次确认。

## MongoDB 第一版实际实现

- 通过 SSH 隧道访问 MongoDB 本地端口。
- 使用 MongoDB.Driver 执行连接测试、版本读取和用户列表读取。
- 支持认证数据库、目标数据库和角色选择。
- 支持创建用户、编辑角色、重置密码和删除普通用户。
- 新用户和密码通过登录验证后保存到保险库。
- 当前管理账号不能删除、编辑角色或重置密码。
- 使用 `mongodump --archive --gzip` 生成备份，服务器临时归档下载后校验 SHA-256 并清理。
- 使用 `mongorestore --archive --gzip` 恢复本机归档；恢复前明确提示可能覆盖同名集合。
- 本机未安装 MongoDB Database Tools 时只提示缺少工具，不静默下载。

MongoDB 的角色和认证库不使用 MySQL 的 GRANT 语法，界面和服务层保持独立适配。

## 备份传输安全实现

- MySQL/MariaDB、MongoDB 和 Redis RDB 均使用服务器一次性临时文件加 SFTP 下载。
- 下载后校验文件大小和 SHA-256。
- 数据库密码通过一次性临时凭据文件传递，不放入命令行参数。
- 任务成功、失败或取消时清理服务器临时文件、本机临时文件和本机导入进程。
- MongoDB 备份使用 BSON archive，不与 SQL 备份格式混用。
- Redis AOF 多文件恢复暂不自动执行，当前仅开放 RDB 导出，避免将不完整 AOF 当成可恢复备份。

## 十四、逻辑阶段的建议顺序

界面冻结后，建议按以下顺序填充逻辑：

1. 数据库服务检测和状态刷新。
2. SSH 临时隧道建立与关闭。
3. 数据库凭据测试和保险库存取。
4. MySQL / MariaDB 连接测试。
5. MySQL / MariaDB 用户创建、权限分配和登录验证。
6. MySQL / MariaDB 备份与远程到本地迁移。
7. MongoDB 用户和角色管理。
8. MongoDB 备份与迁移。
9. Redis ACL 管理和 RDB/AOF 迁移。
10. 失败回滚、临时文件清理和操作日志。
11. Oracle 数据库管理功能。

## 十五、阶段结论

数据库模块适合拆成两个阶段：

### 阶段一：界面和交互确认

- 只做按钮、窗口、面板、列表、弹窗和模拟状态。
- 不连接真实数据库。
- 不修改远程服务器。
- 先确认用户使用起来是否直观。

### 阶段二：逐个填充真实逻辑

- 先完成 MySQL / MariaDB。
- 再完成 MongoDB。
- 再完成 Redis。
- Oracle 保持占位，最后单独评估。

该方案可以最大限度降低反复返工，界面确定后再让每个数据库适配器按照固定位置接入自己的逻辑。
