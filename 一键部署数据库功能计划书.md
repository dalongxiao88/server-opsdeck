# 小白服务器管理器：一键部署数据库功能计划书

版本：v1.1 首期逻辑实现稿  
更新时间：2026-09-05  
当前阶段：界面已验收，MySQL、MariaDB、MongoDB、Redis 一键部署逻辑已接入并完成测试服务器闭环验证

## 一、功能定位

在数据库管理窗口内提供唯一的“部署数据库”入口，用于在目标 Windows 服务器上直接下载、安装和初始化常用数据库。

该功能不是普通的“新建逻辑库”，而是完整的数据库实例部署流程，包括：

- 检查远程服务器环境。
- 下载并校验数据库安装包。
- 初始化数据目录和管理账号。
- 注册并启动 Windows 服务。
- 创建初始空数据库或逻辑库。
- 通过 SSH 隧道验证连接。
- 将验证后的数据库凭据保存到 AES-256-GCM 保险库。
- 清理远程临时文件。

核心原则：

- 不安装 Agent。
- 不使用 Relay 或中转服务。
- 默认不开放数据库公网端口。
- 下载和安装动作在目标服务器执行。
- 本机管理器只负责下发命令、显示进度、验证连接和保存凭据。
- 明文 `servers.xml` 模式不开放一键部署；必须使用加密保险库。
- 不覆盖或删除服务器已有的数据库实例。

## 二、支持范围

第一期只提供经过验证的常用版本：

| 数据库 | 第一组选项 | 说明 |
|---|---|---|
| MySQL | 8.4 LTS、8.0 | 官方 Windows 发行版，覆盖新项目和常规兼容场景 |
| MariaDB | 11.4 LTS、10.11 LTS | 官方 Windows 发行版 |
| MongoDB | 8.0、7.0 | 同步准备匹配的 Database Tools |
| Redis | 8.x、7.x | Windows 社区构建，界面必须明确提示其性质 |
| Oracle | 暂不支持 | 保留显示，不进入部署流程 |

旧版本和特殊版本不纳入一键部署。使用者可以自行在服务器上安装，安装完成后仍可使用数据库检测、连接、用户、权限、备份和迁移功能。

如果检测到同类型数据库服务，默认禁止覆盖安装。后续如确有需要，可以另行增加“独立实例”模式。

## 三、界面入口

数据库管理窗口顶部统计区右侧增加唯一主入口：

~~~text
[数据库服务] [已验证] [待配置]       [部署数据库]
~~~

左下角不再保留重复的“添加数据库”按钮，只保留“刷新检测”。

点击“部署数据库”后打开居中的二级向导，不改变主窗口布局。

## 四、部署向导界面

### 4.1 选择数据库

显示数据库类型、推荐版本、支持状态和简短说明：

- MySQL：8.4 LTS（推荐）、8.0（兼容）。
- MariaDB：11.4 LTS（推荐）、10.11 LTS（兼容）。
- MongoDB：8.0（推荐）、7.0（兼容）。
- Redis：8.x（推荐）、7.x（兼容，Windows 社区构建）。
- Oracle：开发中，不可选择。

不默认选择任何数据库类型，用户必须明确选择后才能继续。

### 4.2 版本与初始化配置

根据所选数据库动态展示配置字段：

~~~text
数据库类型：MariaDB
版本：       11.4 LTS
服务名称：   MariaDB
监听地址：   127.0.0.1
端口：       3306          [探测端口] [随机端口]
初始数据库： app_database
管理账号：   root
管理密码：   [自动生成]    [显示 5 秒] [重新生成]
~~~

所有端口必须在服务器端探测，不能只检查本机端口。默认端口已被占用时，应提示并推荐空闲端口。

### 4.3 安全确认

显示最终部署摘要：

- 目标服务器名称和隐藏后的 IP。
- 数据库类型和精确版本。
- 服务名称、监听地址和端口。
- 初始数据库、账号和密码保存策略。
- 公网端口默认不开放。
- 安装动作将在目标服务器执行。

勾选“我已确认以上配置”后，才启用“开始部署”。

### 4.4 部署进度

部署阶段复用现有的二级进度窗口：

~~~text
○ 检查服务器环境
○ 检查端口与现有服务
○ 下载并校验安装包
○ 初始化数据库
○ 注册并启动服务
○ 创建管理账号与初始数据库
○ SSH 隧道连接验证
○ 保存凭据到保险库
○ 清理临时资源
~~~

进度窗口已经接入真实部署状态。用户在安全确认后仍需在进度窗口点击“确认执行”，才会开始远程下载和安装。

## 五、远程执行方案

默认流程：

~~~text
本机管理器
    ↓ SSH
目标服务器从官方地址直接下载
    ↓
目标服务器校验 SHA-256
    ↓
解压、初始化、注册服务
    ↓
创建账号和初始数据库
    ↓
本机通过 SSH 隧道验证
    ↓
凭据保存到 servers.vault
~~~

服务器无法访问官方源时，后续可以增加备用流程：本机下载并校验，再通过 SFTP 上传到服务器。第一期界面预留提示，但不要求同时实现备用下载流程。

## 六、安全和回滚

- 安装包只接受内置或签名版本清单中的地址和 SHA-256。
- 默认只监听 `127.0.0.1`。
- 默认不创建公网防火墙放行规则。
- 密码不写入命令行、脚本文件、日志或错误提示。
- 密码只在验证成功后保存到 `servers.vault`。
- 安装失败时只回滚本次创建的服务、目录、账号和防火墙规则。
- 不允许误删已有同名服务、数据库目录或用户资料。
- 记录“由管理器部署”的标识，未来卸载功能只允许处理带有该标识的实例。

## 七、实现顺序

1. 完成入口按钮和部署向导界面。
2. 确认数据库类型、版本、端口和初始化字段的布局。
3. 完成部署摘要和安全确认交互。
4. 完成进度窗口的阶段展示。
5. 已实现 MariaDB 完整下载安装、初始化、验证和回滚。
6. 已复用框架接入 MySQL、MongoDB 和 Redis。
7. 已使用测试服务器逐项完成部署、连接验证和回滚闭环。
8. 后续再评估本机备用下载和一键卸载。

## 八、验收标准

界面阶段：

- 入口清晰，页面上只有一个部署入口。
- 四种可部署数据库的版本选项清楚且不默认误选。
- Oracle 显示开发中并不可选。
- 配置字段随数据库类型变化。
- 端口、账号、密码和初始数据库字段不被遮挡。
- 默认窗口和最小窗口尺寸下没有控件重叠。
- 未勾选安全确认时不能进入部署。

逻辑阶段：

- 安装动作确实在目标服务器执行。
- 安装失败自动回滚且无临时文件残留。
- 数据库只监听回环地址。
- 本机经 SSH 隧道登录成功。
- 验证后的账号和密码只写入加密保险库。
- 不影响服务器原有数据库实例和现有管理功能。

## 九、首期实现结果

当前已接入：

- 目标服务器环境、管理员权限、磁盘空间检查。
- 同类型数据库、服务名称、安装目录、数据目录和监听端口冲突检查。
- 服务器端从受控 HTTPS 地址直接下载。
- 主安装包及 MongoDB Database Tools 的固定 SHA-256 校验。
- MySQL、MariaDB、MongoDB、Redis 的独立安装目录和数据目录。
- Windows 服务注册、启动状态和端口监听验证。
- 通过 SSH 隧道初始化管理账号和初始空库。
- MongoDB 与 Redis 初始化完成后启用认证并重启验证。
- 使用最终账号重新建立 SSH 隧道，读取真实数据库版本。
- 数据库凭据和部署元数据写入 `servers.vault`。
- 部署成功后自动刷新数据库列表并选中新实例。
- 远程安装包、解压缓存和临时资源清理。
- 失败时按照部署所有权标记执行严格回滚。

受控精确版本：

| 界面版本 | 精确版本 |
|---|---|
| MySQL 8.4 LTS | 8.4.7 |
| MySQL 8.0 | 8.0.46 |
| MariaDB 11.4 LTS | 11.4.8 |
| MariaDB 10.11 LTS | 10.11.14 |
| MongoDB 8.0 | 8.0.12 |
| MongoDB 7.0 | 7.0.22 |
| Redis 8.x | 8.10.1 Windows 社区构建 |
| Redis 7.x | 7.4.8 Windows 社区构建 |

测试服务器已完成以下非破坏性闭环：

- MariaDB 11.4.8：下载、校验、初始化、设密、建库、SSH 隧道登录、版本读取、回滚。
- Redis 8.10.1：下载、校验、ACL 管理账号、空逻辑库、禁用无密码用户、SSH 隧道登录、版本读取、回滚。
- MongoDB 8.0.12：下载、校验、管理账号、初始数据库、启用认证、SSH 隧道登录、备份工具发现、备份导出、回滚。
- MySQL 8.4.7：在不停止现有 MySQL80 的前提下，使用独立端口和目录完成初始化、设密、建库、登录验证和回滚。
- 正常入口检测到同类型数据库时会在下载前阻止部署。
- 预先存在的目录不会被预检失败后的回滚删除。
- 回滚必须匹配管理器写入的部署所有权标记。

暂未接入：

- 服务器无法访问官方源时的“本机下载后 SFTP 上传”备用流程。
- 一键升级数据库。
- 一键卸载管理器部署的数据库。
- Oracle 自动部署。
