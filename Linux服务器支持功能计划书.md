# Linux 服务器支持功能计划书

版本：v1.1 第一阶段实现稿
更新时间：2026-09-05  
适用项目：小白服务器管理器

## 一、建设目标

在保留现有 Windows 管理能力的基础上，为 Linux 服务器增加统一的远程管理能力。Linux 端不部署 Agent，不依赖中转服务器，所有操作通过现有的 SSH 连接直接完成。

目标是让用户在同一个服务器列表中管理 Windows 和 Linux 服务器，同时根据目标系统自动显示可用功能，避免为 Linux 重新制作一套独立的管理器界面。

## 二、明确边界

### 首期支持

- SSH 登录、连接状态和实时延迟
- Linux 系统信息读取
- 远程重启及重启结果验证
- SSH 端口安全修改与失败回滚
- 常用 HTTP/HTTPS 端口检测与修改
- MySQL、MariaDB、Redis、MongoDB 的数据库管理
- 通过 SSH 使用目标服务器自身的包管理器进行数据库部署

### 首期不支持

- 不部署 Agent 服务
- 不使用 Relay、中转服务器或常驻后台服务
- 不提供一键卸载数据库
- 不提供远程删除数据库目录或删除数据库数据
- Oracle 数据库管理和一键部署暂不实现，仅保留后续接口
- 不默认安装或启用 xrdp、VNC 等远程桌面服务
- 不承诺覆盖所有 Linux 发行版和所有第三方发行版

## 三、首期目标发行版

首期只针对常见且命令差异相对清晰的发行版进行验证：

| 系列 | 首期版本 | 包管理器 | 防火墙重点 |
|---|---|---|---|
| Ubuntu | 22.04、24.04 LTS | apt | ufw、nftables |
| Debian | 12 | apt | nftables、ufw |
| Rocky Linux | 9 | dnf | firewalld、nftables |
| AlmaLinux | 9 | dnf | firewalld、nftables |

CentOS 7、较旧 Ubuntu、国产发行版和特殊定制系统在首期不作为自动修改目标。识别到未验证的系统时，仍可使用基础 SSH 连接，但高风险自动化功能应提示风险并停止执行。

## 四、总体架构

### 1. 系统识别层

SSH 连接成功后读取 `/etc/os-release`、内核信息、架构、当前用户和主机名，建立 Linux 系统画像：

- 发行版及版本
- CPU 架构
- 当前用户是否为 root
- 是否具备可用的 sudo 权限
- 包管理器类型
- 防火墙类型
- systemd 是否可用
- 当前 SSH 服务端口

系统画像需要缓存到当前运行会话，但不保存服务器密码或 sudo 密码到普通配置文件。必要的数据库凭据仍只进入 `servers.vault`。

### 2. Linux 远程执行层

在现有 SSH 客户端之上增加统一的 Linux 命令执行接口，至少区分：

- 普通命令执行
- root 命令执行
- sudo 命令执行
- 文件读取和受控文件上传
- 带超时、退出码、标准输出和标准错误的结果

命令必须使用参数化或经过严格转义的值拼接，禁止把未经验证的端口、路径、用户名直接拼成可执行脚本。每项高风险操作都要保留操作前备份、操作后验证和失败回滚步骤。

### 3. 发行版适配器

不要在业务代码中散落大量发行版判断，建议拆分为适配器：

- `LinuxPackageManagerAdapter`：apt、dnf
- `LinuxFirewallAdapter`：ufw、firewalld、nftables
- `LinuxServiceAdapter`：systemd 服务状态、启动、停止、重载
- `LinuxSshConfigAdapter`：sshd 配置检测、备份、修改和校验
- `LinuxDatabaseAdapter`：各数据库的配置路径、服务名和重启方式

适配器先探测目标环境，再决定是否允许执行；探测不到可靠的适配器时只提供查看功能。

## 五、功能分阶段实现

### 阶段一：基础连接和系统信息

#### 功能

- 服务器新增时选择 Windows、Linux 或自动识别
- Linux 使用 SSH 密码登录
- 支持 SSH 私钥登录，私钥只从用户选择的本机文件读取
- 支持带口令的私钥
- 读取发行版、版本、内核、架构、CPU、内存、磁盘和运行时间
- 复用现有 30 秒延迟检测
- 在主列表显示系统类型和 SSH 状态

#### 验收标准

- Ubuntu、Debian、Rocky Linux、AlmaLinux 均能正确识别
- SSH 失败时显示可理解的错误原因
- 不具备 sudo 时不会误报为管理员权限
- 私钥内容不会写入日志、明文配置或服务器列表文件

### 阶段二：Linux 远程重启

#### 逻辑

1. 显示二次确认窗口，明确目标服务器和当前连接方式。
2. 检查当前账号是否为 root 或能执行 `sudo -n systemctl reboot`。
3. 发送重启指令，记录“指令已发送”状态。
4. 等待 SSH 断开或目标暂时不可达。
5. 按现有延迟检测机制轮询 Ping 和 SSH。
6. 目标恢复后重新建立 SSH 连接，确认系统已经恢复。
7. 在二级进度窗口显示发送、离线、恢复、验证完成或超时状态。

#### 安全要求

- 不因为 Ping 恢复就直接判定重启完成，还要验证 SSH 恢复。
- 重启超时只报告状态，不重复发送重启命令。
- 默认超时时间为 90 秒，后续可配置。
- 不提供 Linux 关机按钮，避免远程关机后无法通过本工具恢复。

### 阶段三：SSH 端口修改

#### 推荐流程

1. 检测目标端口是否位于 1-65535，是否已经被其他进程占用。
2. 识别防火墙类型和 SSH 服务配置位置。
3. 记录原始 `sshd_config` 和防火墙规则备份。
4. 自动检测本机公网出口 IP，并在需要时仅临时放行该 IP；无法可靠判断时要求用户确认。
5. 临时开放备用 SSH 端口，例如 22222，并确认备用 SSH 连接可用。
6. 修改 SSH 主端口，执行 `sshd -t` 语法检查。
7. 重载或重启 SSH 服务。
8. 通过备用端口验证新端口 SSH 登录。
9. 新端口验证成功后，再关闭旧端口和临时备用端口。
10. 任一步骤失败，使用备份恢复 SSH 配置和防火墙规则，并通过备用连接验证回滚结果。

#### 必须防止的情况

- 不允许在没有备用连接或无法验证回滚路径时直接关闭旧 SSH 端口。
- 不允许把本机 IP 识别失败当作“已放行”。
- 不允许只修改配置文件而不测试 `sshd -t`。
- 不允许在新连接验证前关闭临时备用端口。
- 云厂商安全组无法通过服务器本机 SSH 自动修改时，必须明确提示用户手动放行。

### 阶段四：常规端口管理

首期支持 SSH、HTTP、HTTPS 和已识别的数据库端口。端口修改统一使用“探测、备份、修改、验证、回滚”的事务式流程。

- HTTP/HTTPS：修改前提示用户确认服务配置文件中的实际端口
- 未识别服务：只允许探测，不提供自动修改
- 端口输入框激活前快速探测端口占用情况
- 提供随机选择空闲端口按钮
- 端口冲突、服务配置不一致或重启失败时停止并回滚
- 不主动修改云厂商外部安全组，除非后续接入明确的厂商 API

### 阶段五：数据库管理和一键部署

现有数据库管理页面尽量复用，后端根据系统类型切换 Linux 适配器。

#### 管理功能

- MySQL / MariaDB：用户、权限、密码、备份、恢复、迁移
- Redis：ACL 用户、权限、RDB 备份
- MongoDB：用户、角色、备份、恢复、迁移
- Oracle：保留入口并提示“正在开发中”

#### 一键部署

- 由目标服务器下载软件包或使用发行版仓库
- 安装过程通过 SSH 执行，不依赖本机上传大文件
- 安装前检查系统版本、架构、磁盘空间、端口和同类型数据库
- 默认只监听 `127.0.0.1`，不主动暴露数据库公网端口
- 初始化管理员账号、空库、服务自启动和连接验证
- 成功后把数据库凭据和部署元数据写入 `servers.vault`
- 失败时只回滚本次部署创建的服务、配置和目录
- 不提供面向用户的一键卸载或删除入口

## 六、界面调整

### 主列表

- 增加系统类型列：Windows / Linux
- 增加连接方式列：RDP、SSH、WinRM
- Linux 服务器隐藏不适用的 RDP 和 WinRM 操作
- 状态灯继续使用真实探测结果，不使用固定颜色伪装在线状态
- 右键菜单根据系统和探测能力动态显示

### 二级窗口

- Linux 系统信息
- Linux 端口管理
- Linux 重启确认和状态验证
- Linux 数据库部署
- Linux 数据库管理

主窗口不预留大量 Linux 专用空间，复杂操作全部放在二级窗口中，继续复用现有的进度窗口和确认流程。

## 七、权限与凭据策略

- 优先支持 root 或明确的 sudo 管理账户
- 无 sudo 权限时，允许连接和查看，但禁止执行需要提权的操作
- sudo 密码只在当前操作生命周期内使用，不写入日志
- SSH 私钥凭据必须进入现有加密保险库或由用户每次选择
- 数据库密码只允许保存到 `servers.vault`
- 明文 `servers.xml` 模式不开放高风险自动部署和需要长期保存的数据库凭据
- 所有远程命令禁止输出密码、私钥和完整连接字符串

## 八、测试计划

### 系统测试

- Ubuntu 22.04、Ubuntu 24.04
- Debian 12
- Rocky Linux 9
- AlmaLinux 9

### 功能测试

- SSH 密码登录和私钥登录
- 普通用户、sudo 用户、root 用户
- 无 sudo 权限的只读场景
- 重启成功、重启超时、SSH 恢复失败
- SSH 端口修改成功、端口冲突、语法错误、回滚
- ufw、firewalld、nftables 和无防火墙环境
- 数据库服务已安装、未安装、端口被占用和配置文件缺失
- SSH 连接中断、网络抖动和重复点击保护

### 安全测试

- 命令注入和特殊字符参数
- 端口范围和路径穿越
- 保险库中是否出现明文密码
- 日志、错误窗口和进度窗口是否泄露凭据
- 失败回滚后是否仍能通过原 SSH 端口登录
- 不误改手动安装的数据库和用户目录

## 九、开发顺序和完成标准

建议按以下顺序开发：

1. Linux 系统识别和 SSH 私钥登录
2. Linux 系统信息和主列表展示
3. Linux 远程重启及状态验证
4. SSH 端口修改和回滚
5. 常规端口管理
6. 数据库管理适配
7. Linux 数据库一键部署
8. 多发行版回归测试和文档整理

首期 Linux 支持完成的标准：

- 四类目标发行版均能完成识别和 SSH 管理
- Linux 重启具备发送、离线、恢复、验证和超时状态
- SSH 端口修改具备备用通道和失败回滚
- 不部署 Agent，不需要中转服务器
- 不写入明文服务器密码和数据库密码
- Windows 现有功能不回归
- 发布前完成源码构建、功能回归和敏感信息扫描

## 十、当前实现状态

截至本计划书更新时间，代码已完成以下部分：

- Linux 服务器类型、SSH 密码认证和 SSH 私钥认证
- 私钥文件选择、带口令私钥验证和加密保险库保存
- Linux 发行版、内核、架构、CPU、内存、磁盘、systemd、sudo、防火墙和 SSH 端口画像
- Linux 系统信息二级窗口
- Linux 远程重启及 Ping/SSH/启动时间恢复验证
- Linux SSH 端口双端口迁移、端口冲突检查、配置语法校验、防火墙规则和失败回滚
- Linux MySQL/MariaDB、MongoDB、Redis 数据库端口修改
- Linux Nginx、Apache2/httpd 的 HTTP/HTTPS 端口修改
- Linux MySQL/MariaDB、MongoDB、Redis 服务发现
- 通过 SSH 隧道复用数据库版本、用户、权限等管理能力
- MySQL/MariaDB、MongoDB、Redis 的 Linux 远程备份下载路径
- 21 项基础本地自测、6 项数据库/Web 端口脚本测试和新增窗口的位图渲染检查

以下内容仍需接入 Linux 测试机后继续实现或验收：

- Ubuntu、Debian、Rocky Linux、AlmaLinux 实机 SSH 连接和重启回归
- 四类发行版的防火墙、SELinux 和 SSH 端口迁移实机验证
- Linux 数据库备份的真实权限、服务路径和工具版本回归
- Linux 数据库一键部署
- Linux 数据库恢复和迁移的本机目标适配
